#pragma once

// Enable this to output all the per layer inputs and outputs

//#define PRINT_DEBUG

// Enable this to dump timing information per layer.
// Enabling this along with the above will mess up timing. 

//#define TIMING
